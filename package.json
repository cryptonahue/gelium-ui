{
  "name": "gelium-ui-monorepo",
  "version": "0.5.0",
  "private": true,
  "workspaces": [
    "lib",
    "site"
  ],
  "scripts": {
    "build": "npm run build -w gelium-ui-site && node scripts/build-lib-dist.mjs",
    "release:check": "node scripts/check-release.mjs",
    "test:workspace-resolution": "node scripts/verify-workspace-gelium-resolution.mjs"
  }
}